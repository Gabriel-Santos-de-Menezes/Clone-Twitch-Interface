import React from 'react';

import {Text} from 'react-native';
import { Wrapper, Heading } from './styles';

const ComingSoon: React.FC = () => {
    return (
        <Wrapper>
            <Heading>Coming sonn...</Heading>
        </Wrapper>
    );
};

export default ComingSoon
